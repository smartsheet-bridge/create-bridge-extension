---
id: quick_start
title: Quick Start
sidebar_label: Quick Start
---

## Create Extension

:::warning

This page is incomplete!

Thank you for your patience as the team works to improve extension onboarding. 

Please refer to the [Manual Setup](/advanced/setup) page to get started.

:::

:::note

To start developing locally you will need [`NodeJS`](https://nodejs.org/en/) installed on your development machine along with either `npm` or `yarn` and good knowledge of developing `JavaScript` or `TypeScript` applications and services.

:::

Please refer to the [Manual Setup](/advanced/setup) page.

<!-- The recommended way to start developing a new Extension is to use the official `create-bridge-extension` tool along with either `npm` or `yarn` to scaffold a new extension project.

<Tabs
groupId="js2ts"
defaultValue="js"
values={[
  { label: 'JavaScript', value: 'js', },
  { label: 'TypeScript', value: 'ts', },
]
}>
<TabItem value="js">


Use `npm`

```bash
npx @smartsheet-bridge/create-extension hello_world
```

or `yarn`

```bash
yarn create @smartsheet-bridge/extension hello_world
```

</TabItem>
<TabItem value="ts">


Use `npm`

```bash
npx @smartsheet-bridge/create-extension hello_world --template=ts
```

or `yarn`

```bash
yarn create @smartsheet-bridge/extension hello_world --template=ts
```

</TabItem>
</Tabs>


The tool will scaffold a very basic, but complete, extension project in the folder `hello_world`. This extension is ready to be deployed. -->


